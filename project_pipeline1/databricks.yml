# This is a Databricks asset bundle definition for project_pipeline1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: project_pipeline1
  uuid: d6f49005-0b5f-46c2-ac66-df34628968a4

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-4408969214176250.10.azuredatabricks.net
    variables:
      catalog: dlt_catalog
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-4408969214176250.10.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/jenny@xgao198902gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jenny@xgao198902gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dlt_catalog
      schema: prod
    permissions:
      - user_name: jenny@xgao198902gmail.onmicrosoft.com
        level: CAN_MANAGE
